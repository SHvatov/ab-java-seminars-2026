# Семинар №12 - Executor Framework

# Оригинальный план семинара

### Список тем

1. ThreadPool и ExecutorService
2. CompletableFuture и его методы
3. Иммутабельные структуры данных
4. Потокобезопасные коллекции
5. BlockingQueue и неблокирующие очереди
6. Producer-Consumer
7. Модель leader/follower
8. Работа с ForkJoinPool

### ThreadPool и ExecutorService

1. Представление примера с использованием ExecutorService для создания пула потоков.
2. Обсуждение различных способов создания ThreadPool и их применения.
3. Работа с Future для ожидания результатов вычислений.

### CompletableFuture

1. Демонстрация примера использования CompletableFuture для асинхронной работы.
2. Методы `thenAccept`, `thenApply`, `exceptionally` и их использование в цепочке обработки.
3. Практическое задание: преобразование результатов асинхронных операций.

### Иммутабельные структуры данных и Потокобезопасные коллекции

1. Объяснение концепции иммутабельности и её важности в многопоточных приложениях.
2. Переход к потокобезопасным коллекциям.
3. Сравнение `HashMap` и `ConcurrentHashMap` в контексте многопоточности.
4. Описание потенциальных проблем с `HashMap` и демонстрация преимуществ `ConcurrentHashMap`.
5. Разбор примеров использования `CopyOnWriteArrayList` и `ConcurrentHashMap`.

### BlockingQueue и неблокирующие очереди

1. Рассмотрение разницы между блокирующими и неблокирующими очередями.
2. Демонстрация использования `BlockingQueue` для синхронизации задач между потоками.

### Пример Producer-Consumer

1. Создание приложения producer-consumer с использованием `BlockingQueue`.
2. Анализ проблемы производителей и потребителей и методов её решения.

### Модель leader/follower

1. Объяснение модели leader/follower и сценарии её использования.
2. Пример реализации модели leader/follower в Java.

### Задачи для ForkJoinPool

1. Описание принципов работы ForkJoinPool.
2. Рассмотрение задач, эффективно решаемых с помощью ForkJoinPool.
3. Проведение практического занятия по разработке простого приложения с использованием ForkJoinPool.

# Мой план

### Синхронизированные и конкурентные коллекции

1. Слайды 5-6. Рассказать почему классические коллекции не особо хорошо себя демонстрируют в многопоточных программах. Тест - *ConcurrentMapsTest.* В несколько потоков вставляем ключ в мапу. Ожидаем, что только в одном потоке ключа в мапе не будет. 
2. Слайд 7. Продемонстрировать все многообразие синхронизированных и concurrent-коллекций в нативной Java. На семинаре мы познакомимся не со всеми - все, что фиолетовое - для самостоятельного ознакомления. 
3. Далее переходим к тестам. 
    1. Сначала - *ConcurrentMapsTest.* Пример со слайдов можно не демонстрировать, но стоит показать второй тест, где сравнивается производительность всех трех типов мап. Объяснить, почему у синхронизированного варианта самая медленная скорость. По желанию можно рассказать про то, как достигли высокой производительности в ConcurrentHashMap (путем блокирования только определенных бакетов - [https://www.baeldung.com/java-concurrent-map](https://www.baeldung.com/java-concurrent-map)). 
    2. Далее - *BlockingQueueTest*. Там два примера - с блокирующимися и неблокирующимися продьюсером и консьюмером на основе LinkedBlockingQueue. Рассказать про методы take, poll, put и offer - в чем разница, когда какой стоит использовать. 
4. Слайды 9 - 10. Рассказать про разницу между двумя очередями (все написано на слайде 10 - [https://www.baeldung.com/java-queue-linkedblocking-concurrentlinked)](https://www.baeldung.com/java-queue-linkedblocking-concurrentlinked)
5. Слайд 11 - Кратко подвести итоги, когда какие коллекции использовать. 

### Синхронизаторы

Слайды 13 - 17. Кратко рассказать, что такое синхронизаторы и для чего они нужны. Пробежаться по тем, что предложены для изучения на семинаре. 

Тест - *SyncTest:*

- CountDownLatch - с его помощью ждем инициализации всей информации о клиенте при сборе ее из разных мест. 
- Exchanger - обмен секретными данными между потоками. 
- Lock - блокировка на запись и чтение. Три потока читают значение неатомарного счетчика, и один с какой-то частотой его блокируют. Рассказать про tryLock - в чем смысл, когда может быть полезен. В принципе, можно явно подсветить ребятам, что не стоит использовать блокирующие операции без таймаута, ибо может привести к зависанию системы.  

### Executor Framework & Future

Слайды 20-23. Рассказать про основные компоненты, которые входят во Фреймворк. 

Здесь же можно затронуть тему пулов потоков. Поток - штука тяжеловесная, соответственно их лучше не порождать по штуке на задачу (привет, Loom!), а создать заранее и переиспользовать. 

Практика:

1. *ExecutorTest* - показать, как работать с экзекуторами. Как их создавать, как их закрывать, как сабмитить задачи - рассказать про разницу между invoke и submit. 
2. *FutureTest* - плавно переходим к Future, как способу отслеживания выполнения асинхронных задач. Рассказать про состояния, базовый интерфейс - как получить значение, как отменить задачу, как узнать о статусе ее выполнения. В конце продемонстрировать несколько способов дождаться выполнения одной или более асинхронных задач. 

### CompletableFuture & ForkJoinPool

Слайды 26-27. Рассказать про асинхронное выполнение задач, в качестве примера привести Ноду и Томкат. Рассмотреть, какие есть преимущества и недостатки у обоих подходов. 

Слайды 28-31. CompletableFuture - как следующая этап развития асинхронщины в Java 8. Все еще наследует от Future, но с помощью методов из CompletionStage можно строить гораздо более сложные пайплайны. 

Практика:

- *CompletableFutureTest* - рассказать, как CF можно создавать с помощью фабричных методов из CF. Далее продемонстрировать две ипостаси CF - и фьюча, и стейдж. Можно рассказать, как оно там под капотом складывается в стейджи ([https://habr.com/ru/articles/784144/](https://habr.com/ru/articles/784144/)). Далее продемонстрировать, как помимо обычного завершения самой задачи, можно заранее завершить CF - complete, timeout, cancel. Обратить внимание ребят на то, что под капотом задача все еще исполняется, просто ее результат нам уже не важен. Затем продемонстрировать, как пропагируются исключения в CF - сверху вниз и не наоборот. В конце поговорить про разницу обычных методов от Async и где реально исполняются задачи. Сделать кивок в сторону ForkJoinPool. 
- *CompletableFuturePipelineTest* - достаточно сложный пример, в котором рекурсивно по мере завершения фьюч идет обработка их результатов в другой фьюче. 

Слайд 34. Кратко затронуть тему ForkJoinPool. Что это за Executor, в чем его основная особенность, где используется и почему некоторыми считается антипаттерном. 