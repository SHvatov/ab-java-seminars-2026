# Семинар 2. Чеклист семинариста

**До семинара:**

- [ ] Убедиться, что презентация (SOLID → CLI-каркас → паттерны GoF → Factory Method → Iterator → Strategy) есть в
  репозитории и отображается корректно.
- [ ] Проверить исходный код семинара: проект собирается; `StockFactory`/`BondFactory`/`EtfFactory.create()`
  бросают исключение — ожидаемо, реализуются на занятии (этап 4). В `filter/` на этом этапе — только интерфейс
  `InstrumentFilter` с ФТ1–ФТ7 в Javadoc, ни одной реализации; `TypeFilter`/`NoOpFilter`/`TickerFilter`/
  `CurrencyFilter`/`PriceFilter`/`FilterFactory` появляются только в эталоне. Остальной код — доменная модель,
  CLI-каркас, `Iterator` — уже рабочий, ничего из этого не пишется заново.
- [ ] Подготовить у себя готовую реализацию `StockFactory`/`BondFactory`/`EtfFactory`, всех пяти фильтров и
  `FilterFactory` — эталон для сверки по ходу семинара; студентам заранее не показывать.
- [ ] Подготовить доску (WhiteBoard / доска в Толке) для фиксации таблицы «задача → паттерн-кандидат» (этап 3).

**По ходу семинара:**

### Этап 1. Теория: принципы SOLID

- [ ] Провести теорию по материалу «Принципы SOLID»: LSP → ISP → SRP → OCP → DIP (обзорно) → KISS и DRY.
- [ ] Показать, что `getDividends` уже объявлен только в `Stock` (семинар 1) — LSP не нарушен; интерфейс
  `DividendPaying` лишь называет эту способность явно (ISP).
- [ ] Явно оговорить: полноценный DIP через dependency injection — тема следующего учебного года (Spring), не этого
  курса; пример на своём коде — конструкторная передача `repository` в CLI-команды.

### Этап 2. CLI-каркас и подключение picocli

- [ ] Показать `pom.xml`, объяснить его назначение по материалу «Maven и pom.xml» — включая, зачем вообще нужны
  системы сборки.
- [ ] Показать зависимость picocli, обзорно — основные аннотации и классы (`@Command`, `@Option`, `@Parameters`,
  `Callable`, `CommandLine`).
- [ ] Показать весь CLI-каркас целиком: `MarketPulseCli`, `demo/Main`, регистрацию команд через `addSubcommand`.
- [ ] Показать `SearchCommand` как полностью рабочую команду.
- [ ] Показать `AddCommand`/`ListCommand`: код написан целиком, но запустить их пока не получится — внутри фабрик
  (`add`) и фильтров (`list --type`) заглушки. По желанию — запустить `add` вживую и показать исключение из
  `StockFactory`, как повод перейти к следующему этапу.

### Этап 3. Обсуждение: какие паттерны GoF применимы к CLI

- [ ] Провести открытое обсуждение по вопросам из плана, зафиксировать на доске таблицу «задача → паттерн-кандидат».
- [ ] Опереться на уже увиденный на этапе 2 код: Command — уже показанный CLI-каркас; исключения из `add`/
  `list --type` — наглядный повод для вопросов «как создать по строке?» и «как отфильтровать?».
- [ ] Отметить, что Iterator в проекте уже реализован — на занятии его обсуждают и разбирают, а не пишут заново.
- [ ] Кратко показать материал «Паттерны проектирования GoF» — не пересказывать целиком.

### Этап 4. Совместная реализация: Factory Method

- [ ] Реализовать вместе со студентами тела `create()` в `StockFactory`, `BondFactory`, `EtfFactory` — конструирование
  `Stock`/`Bond`/`Etf` по строковым аргументам команды.
- [ ] Обсудить самрегистрацию фабрик через `static`-блок (`InstrumentFactories`) как пример автоматизации добавления
  нового типа.
- [ ] Показать, что `add`, увиденный на этапе 2, теперь работает целиком.

### Этап 5. Итератор по репозиторию — показ и разбор готовой реализации

- [ ] Показать `InstrumentRepository` (`Iterable`/`Iterator`), разобрать на паре эквивалентных фрагментов, что
  `for-each` — синтаксический сахар над `Iterator`.

### Этап 6. Самостоятельная работа: Strategy — фильтрация

- [ ] Направить студентов к Javadoc `InstrumentFilter` — там весь список ФТ1–ФТ7 и примеры ввода, отдельно
  пересказывать не обязательно. В `filter/` пока только этот интерфейс — реализации (`TypeFilter`, `NoOpFilter`,
  `TickerFilter`, `CurrencyFilter`, `PriceFilter`) и недостающие опции `ListCommand` (`--ticker`, `--currency`,
  `--price-op`, `--price`) студенты пишут и заводят сами.
- [ ] `PriceFilter` — единственный, завязанный на конкретный тип (`Stock`); для `Bond`/`Etf` всегда `false`. Убедиться,
  что это не воспринимается как нарушение — здесь это осознанное решение, а не побег от полиморфизма.
- [ ] Сверить результат с эталонной реализацией; отдельно показать `FilterFactory` — как выбор фильтра по опциям и
  проверку ФТ7 (взаимоисключаемость) можно вынести в отдельную фабрику, вторая практика с Factory Method.
- [ ] Закрыть заглушку: показать/заполнить тело `ListCommand.call()` — в эталоне это вызов
  `FilterFactory.create(...)`.
- [ ] После сверки — упомянуть Chain of Responsibility как ответ на вопрос «а если нужно несколько критериев сразу?»
  (не более пары предложений, не тема этого семинара — подробнее на продвинутом треке).

### Этап 7. Пуш и создание MR

- [ ] Запушить рабочую ветку, создать MR / Pull Request.
- [ ] Подвести итоги занятия.
