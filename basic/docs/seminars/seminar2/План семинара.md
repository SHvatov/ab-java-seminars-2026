# Базовый трек

# Семинар 2. Принципы и паттерны проектирования

**Тема модуля:** Принципы дизайна ПО **Тема семинара:** SOLID. Паттерны GoF в CLI-слое приложения.

---

## Пререквизиты

`семинар 1` `лекция по SOLID, KISS, DRY, GoF` `доменная модель Instrument/Stock/Bond/Etf из семинара 1`
`Maven: базовый уровень`

---

## План семинара

| **#** | **Этап**                                                             | **Время**  |
|-------|----------------------------------------------------------------------|------------|
| 1     | Теория: принципы SOLID                                               | ~15–20 мин |
| 2     | Подключение picocli, знакомство с `pom.xml`                          | ~10 мин    |
| 3     | Обсуждение: какие паттерны GoF применимы к CLI                       | ~10–15 мин |
| 4     | Совместная реализация: Factory Method, Strategy                      | ~25–30 мин |
| 5     | Самостоятельная работа: Iterator над `InstrumentRepository`          | ~15 мин    |
| 6     | Разбор решений, эталонная реализация Iterator                        | ~10–15 мин |
| 7     | Совместная реализация: Command поверх picocli, команда `list --type` | ~15–20 мин |

---

### Этап 1. Теория: принципы SOLID (~15–20 мин)

Изложение принципов SOLID в формате мини-лекции (материал
[«Принципы SOLID»](../../materials/seminar2/Принципы%20SOLID.md)), отталкиваясь от нарушения LSP, уже присутствующего в
доменной модели после первого семинара (`getDividends(BigDecimal)` в `Instrument`). Порядок изложения: LSP → ISP (как
проектная причина нарушения LSP) → SRP → OCP (со ссылкой на переход от `instanceof`-цепочки к полиморфизму на
семинаре 1) → DIP —
верхнеуровнево, с явной пометкой, что принцип будет разобран подробно при знакомстве со Spring. Отдельным блоком — KISS
и DRY как менее формальные, но практические ориентиры.

По итогам этапа фиксируется исправление нарушения LSP: `getDividends` выносится из `Instrument` в интерфейс
`DividendPaying`, реализуемый только `Stock`.

---

### Этап 2. Подключение picocli, знакомство с `pom.xml` (~10 мин)

Проект переходит от точки входа `demo.Main` к полноценному CLI. В качестве библиотеки для разбора аргументов командной
строки используется [picocli](https://picocli.info/) — позволяет декларативно описывать команды и опции через аннотации,
не реализуя разбор аргументов вручную.

Перед добавлением зависимости — краткое объяснение назначения `pom.xml` (материал [«Maven и pom.xml»](./Maven.md)):
манифест проекта для Maven, описывающий координаты проекта, зависимости и их версии, плагины сборки. Указание версии
зависимости в одном месте гарантирует, что все участники проекта используют одну и ту же версию библиотеки.

```xml

<dependency>
    <groupId>info.picocli</groupId>
    <artifactId>picocli</artifactId>
    <version>4.7.7</version>
</dependency>
```

После подключения зависимости — краткий обзор основных классов picocli, без разбора отдельного демонстрационного
примера:

```java
/**
 * Основные типы picocli, используемые в проекте:
 *
 * @Command       — аннотация класса-команды (имя, описание, вложенные subcommands)
 * @Option         — аннотация именованного параметра (например, --type)
 * @Parameters     — аннотация позиционного параметра (например, тикер)
 * Callable<Integer> — интерфейс, реализуемый командой; call() возвращает код завершения
 * CommandLine     — точка запуска: разбирает args и вызывает call() нужной команды
 */
```

---

### Этап 3. Обсуждение: какие паттерны GoF применимы к CLI (~10–15 мин)

Формат — открытое обсуждение с группой, а не лекция. Вопросы для обсуждения:

- Какие паттерны проектирования уже знакомы студентам (из прошлого опыта, других курсов, самостоятельного изучения)?
- Как устроена обработка команды в CLI-приложении: что должно происходить, когда пользователь вводит `search AAPL`?
- Как устроено создание объектов `Stock`, `Bond`, `Etf`, когда параметры инструмента приходят не из кода, а из строки,
  введённой пользователем?
- Как отфильтровать список инструментов по типу так, чтобы добавление нового типа инструмента не требовало правки кода
  фильтрации?
- Как перебрать все инструменты в хранилище, не раскрывая наружу, как именно они там хранятся?

Ожидаемое направление ответов и разбор кандидатов:

| **Задача**                                                        | **Паттерн-кандидат** |
|-------------------------------------------------------------------|----------------------|
| Обработка пользовательской команды                                | Command              |
| Создание объекта инструмента по строковому типу                   | Factory Method       |
| Фильтрация списка инструментов без хрупкого сравнения по типу     | Strategy             |
| Обход инструментов в хранилище без раскрытия внутренней структуры | Iterator             |

Обсуждается, что picocli уже частично реализует Command на уровне библиотеки — каждая аннотированная классом `@Command`
команда, реализующая `Callable`, фактически является объектом-командой с методом `call()` в роли `execute()`. Это
наблюдение используется как переход к совместной реализации: паттерн не придумывается с нуля, а узнаётся в уже
используемом инструменте.

Iterator фиксируется в списке кандидатов, но не реализуется совместно — он отдаётся студентам на этапе 5.

После обсуждения — краткий показ материала [«Паттерны проектирования GoF»](./Паттерны%20GoF.md): что такое паттерн
проектирования, три группы (порождающие, структурные, поведенческие) и по какой задаче узнаётся каждый из четырёх
паттернов-кандидатов, зафиксированных в таблице выше. Материал не пересказывается целиком — он служит опорой для
перехода к совместной реализации на этапе 4.

---

### Этап 4. Совместная реализация: Factory Method, Strategy (~25–30 мин)

**Factory Method: создание инструмента по типу.** Пользователь вводит тип инструмента строкой (`STOCK`, `BOND`, `ETF`);
код, создающий объект, не должен разрастаться в `if/else` при добавлении нового типа:

```java
public interface InstrumentFactory {
    Instrument create(String ticker, String name, Currency currency);
}

public final class InstrumentFactories {
    private static final Map<String, InstrumentFactory> REGISTRY = new HashMap<>();

    private InstrumentFactories() {
    }

    public static void register(String type, InstrumentFactory factory) {
        REGISTRY.put(type.toUpperCase(), factory);
    }

    public static Instrument create(String type, String ticker, String name, Currency currency) {
        InstrumentFactory factory = REGISTRY.get(type.toUpperCase());
        if (factory == null) {
            throw new IllegalArgumentException("Unknown instrument type: " + type);
        }
        return factory.create(ticker, name, currency);
    }
}

public class StockFactory implements InstrumentFactory {

    static {
        InstrumentFactories.register("STOCK", new StockFactory());
    }

    @Override
    public Instrument create(String ticker, String name, Currency currency) {
        return new Stock(ticker, name, currency, "Unspecified", BigDecimal.ZERO);
    }
}
```

Подход аналогичен регистрации драйверов в JDBC (`DriverManager`): реестр не содержит статического перечисления всех
фабрик, а каждая фабрика регистрирует себя сама при загрузке своего класса. Добавление нового типа инструмента сводится
к реализации нового `InstrumentFactory` со статическим блоком регистрации — ни `InstrumentFactories`, ни существующие
фабрики не изменяются, что напрямую иллюстрирует OCP, рассмотренный на этапе 1.

**Почему без `loadClass` реестр останется пустым.** Статический блок `StockFactory` выполнится только тогда, когда
JVM реально загрузит этот класс, — а простое упоминание `StockFactory.class` как литерала загрузку не вызывает.
Если ни один код в проекте ни разу не «трогает» `StockFactory` активно (не создаёт его экземпляр, не вызывает его
статический метод), класс так и не загрузится, и его `static`-блок никогда не выполнится — `REGISTRY` останется
пустым, а `InstrumentFactories.create("STOCK", ...)` бросит `IllegalArgumentException`, хотя `StockFactory` физически
лежит в проекте. Поэтому `InstrumentFactories` сама форсирует загрузку каждой известной ей фабрики:

```java
public final class InstrumentFactories {
    private static final Map<String, InstrumentFactory> REGISTRY = new HashMap<>();

    static {
        loadClass(StockFactory.class);
        loadClass(BondFactory.class);
        loadClass(EtfFactory.class);
    }

    // ... register(...), create(...) — без изменений

    private static void loadClass(Class<?> factoryClass) {
        try {
            Class.forName(factoryClass.getName());
        } catch (ClassNotFoundException e) {
            throw new ExceptionInInitializerError(e);
        }
    }
}
```

`Class.forName(String)` — это и есть то самое «активное обращение»: по умолчанию оно инициализирует класс, если он
ещё не инициализирован, тем самым запуская его `static`-блок. Это тот же приём, что исторически использовался с
`DriverManager` до JDBC 4.0: `Class.forName("com.mysql.cj.jdbc.Driver")` в коде выглядел бессмысленно (результат
никуда не присваивался), но форсировал загрузку драйвера ради побочного эффекта его статического блока.

> **Честный компромисс с OCP:** статический блок `InstrumentFactories`, форсирующий загрузку `StockFactory`,
> `BondFactory` и `EtfFactory` (см. итоговый код), при добавлении нового типа инструмента всё равно придётся
> дополнить — а значит, класс `InstrumentFactories` не закрыт для изменения в строгом смысле OCP. Стоит проговорить
> это со студентами прямо: в идеальном мире это место решалось бы через `ServiceLoader` или DI-контейнер (Spring), не
> требуя правки `InstrumentFactories` вовсе — но сегодня осознанно идём на упрощение ради темпа занятия. Продвинутый
> трек показывает `ServiceLoader`-вариант вживую.

**Strategy: фильтрация инструментов по типу.** Наивный способ реализовать фильтр `--type` в будущей команде `list` —
сравнение строки с именем класса напрямую в коде команды:

```java
void example() {
    if (type == null || instrument.getClass().getSimpleName().equalsIgnoreCase(type)) {
        // вывести инструмент
    }
}
```

Такое условие само по себе не нарушает OCP только пока правило фильтрации одно; как только потребуется, например, фильтр
по диапазону цены или по сочетанию признаков, код команды придётся дополнять новыми ветками `if`, вперемешку с уже
существующей логикой. Strategy инкапсулирует условие отбора в отдельный, взаимозаменяемый объект — команда работает с
абстракцией, не зная, по какому именно правилу происходит отбор:

```java
public interface InstrumentFilter {
    boolean matches(Instrument instrument);
}

public class TypeFilter implements InstrumentFilter {
    private final String type;

    public TypeFilter(String type) {
        this.type = type;
    }

    @Override
    public boolean matches(Instrument instrument) {
        return instrument.getClass().getSimpleName().equalsIgnoreCase(type);
    }
}

public class NoOpFilter implements InstrumentFilter {
    @Override
    public boolean matches(Instrument instrument) {
        return true;
    }
}
```

Добавление нового способа отбора (например, `PriceAboveFilter`) сводится к реализации нового `InstrumentFilter` — код,
перебирающий инструменты, не изменяется. Применение паттерна здесь напрямую опирается на этап 1: инкапсуляция алгоритма
отбора в подменяемый объект — это то же соблюдение OCP, что и в Factory Method, но применительно не к созданию объекта,
а к варьируемому поведению.

---

### Этап 5. Самостоятельная работа: Iterator над `InstrumentRepository` (~15 мин)

Задание: в проекте появляется класс `InstrumentRepository` — обёртка над внутренним массивом `Instrument[]`
фиксированного размера; если класс ещё не создан на предыдущих этапах, создать его сейчас. Требуется реализовать
возможность перебора всех инструментов репозитория, не раскрывая наружу сам массив (то есть без метода вида
`getAll(): Instrument[]`). Метод добавления записей (`add(Instrument instrument)`) необходим для наполнения репозитория
перед проверкой перебора — реализовать его вместе с остальным классом, если его ещё нет.

Условия:

- Реализовать собственный тип, предоставляющий методы `hasNext()` и `next()` (пока без реализации стандартного
  интерфейса `java.util.Iterator` — это будет показано на этапе 6).
- Проверить работу реализации классическим циклом:

```java
void example() {
    InstrumentIterator iterator = repository.iterator();
    while (iterator.hasNext()) {
        Instrument instrument = iterator.next();
        System.out.println(instrument.getDescription());
    }
}
```

- `for-each` на этом этапе не использовать — синтаксис ещё не разобран и будет объяснён на этапе 6.

---

### Этап 6. Разбор решений, эталонная реализация Iterator (~10–15 мин)

Разбор одного-двух студенческих решений: как авторы оформили собственный интерфейс `InstrumentIterator` (отдельный
интерфейс, вложенный класс, анонимный класс), как обработана ситуация вызова `next()` после исчерпания элементов.

Эталонная реализация — переход от собственного интерфейса к `java.util.Iterator<Instrument>` и `Iterable<Instrument>`:

```java
public class InstrumentRepository implements Iterable<Instrument> {
    private Instrument[] instruments = new Instrument[100];
    private int size = 0;

    public void add(Instrument instrument) {
        instruments[size++] = instrument;
    }

    @Override
    public Iterator<Instrument> iterator() {
        return new Iterator<>() {
            private int cursor = 0;

            @Override
            public boolean hasNext() {
                return cursor < size;
            }

            @Override
            public Instrument next() {
                if (!hasNext()) {
                    throw new NoSuchElementException();
                }
                return instruments[cursor++];
            }
        };
    }
}
```

Далее — явная демонстрация, что `for-each` является синтаксическим сахаром над `Iterator`, на паре эквивалентных
фрагментов:

```java
void example() {
    // то, что пишет программист
    for (Instrument instrument : repository) {
        System.out.println(instrument.getDescription());
    }

    // во что это разворачивается компилятором
    Iterator<Instrument> iterator = repository.iterator();
    while (iterator.hasNext()) {
        Instrument instrument = iterator.next();
        System.out.println(instrument.getDescription());
    }
}
```

Реализация `Iterable<Instrument>` — условие, необходимое и достаточное для использования `for-each`; без него компилятор
не примет объект в конструкции `for (... : repository)`.

---

### Этап 7. Совместная реализация: Command поверх picocli, команда `list --type` (~15–20 мин)

Первые доменные команды приложения — `SearchCommand` и `AddCommand`, каждая инкапсулирует один сценарий использования и
делегирует получение данных доменному слою, не смешивая парсинг аргументов с бизнес-логикой (связь с SRP, этап 1):

```java

@Command(name = "search", description = "Поиск инструмента по тикеру")
public class SearchCommand implements Callable<Integer> {

    @Parameters(index = "0", description = "Тикер инструмента")
    private String ticker;

    private final InstrumentRepository repository;

    public SearchCommand(InstrumentRepository repository) {
        this.repository = repository;
    }

    @Override
    public Integer call() {
        Instrument instrument = repository.findByTicker(ticker);
        if (instrument == null) {
            System.out.println("Инструмент не найден: " + ticker);
            return 1;
        }
        System.out.println(instrument.getDescription());
        return 0;
    }
}
```

Команда `add`, использующая `InstrumentFactories` из этапа 4 — связывает Factory Method и Command: создание объекта
остаётся ответственностью фабрики, команда лишь разбирает аргументы и передаёт их дальше:

```java

@Command(name = "add", description = "Добавление инструмента в репозиторий")
public class AddCommand implements Callable<Integer> {

    @Parameters(index = "0", description = "Тип инструмента (STOCK, BOND, ETF)")
    private String type;

    @Parameters(index = "1", description = "Тикер инструмента")
    private String ticker;

    @Parameters(index = "2", description = "Название инструмента")
    private String name;

    @Parameters(index = "3", description = "Валюта инструмента (RUB, USD, EUR)")
    private Currency currency;

    private final InstrumentRepository repository;

    public AddCommand(InstrumentRepository repository) {
        this.repository = repository;
    }

    @Override
    public Integer call() {
        Instrument instrument = InstrumentFactories.create(type, ticker, name, currency);
        repository.add(instrument);
        System.out.println("Добавлено: " + instrument.getDescription());
        return 0;
    }
}
```

Команда `list`, использующая итератор из этапа 6 (уже в форме `for-each`, поскольку сахар разобран) и `InstrumentFilter`
из этапа 4 вместо прямого сравнения строк — добавление нового способа фильтрации не потребует изменения кода команды:

```java

@Command(name = "list", description = "Список инструментов")
public class ListCommand implements Callable<Integer> {

    @Option(names = "--type", description = "Фильтр по типу инструмента")
    private String type;

    private final InstrumentRepository repository;

    public ListCommand(InstrumentRepository repository) {
        this.repository = repository;
    }

    @Override
    public Integer call() {
        InstrumentFilter filter = (type == null) ? new NoOpFilter() : new TypeFilter(type);

        for (Instrument instrument : repository) {
            if (filter.matches(instrument)) {
                System.out.println(instrument.getDescription());
            }
        }
        return 0;
    }
}
```

Точка входа приложения создаёт репозиторий и регистрирует команды в корневом объекте picocli:

```java

@Command(name = "market-pulse")
public class MarketPulseCli implements Runnable {
    @Override
    public void run() {
        System.out.println("Используйте --help для списка команд");
    }
}

public class Main {
    public static void main(String[] args) {
        InstrumentRepository repository = new InstrumentRepository();
        CommandLine cli = new CommandLine(new MarketPulseCli())
                .addSubcommand(new SearchCommand(repository))
                .addSubcommand(new AddCommand(repository))
                .addSubcommand(new ListCommand(repository));
        int exitCode = cli.execute(args);
        System.exit(exitCode);
    }
}
```

Команды регистрируются вручную через `addSubcommand`, а не через атрибут `subcommands` аннотации `@Command`: у
`SearchCommand`, `AddCommand` и `ListCommand` нет конструктора без аргументов, и picocli не смог бы создать их
самостоятельно по одному классу — каждой команде нужен уже готовый `repository`.

`InstrumentRepository` на данном этапе — заглушка с захардкоженным набором инструментов; подключение реального источника
данных запланировано на семинар 5.

Стоит отдельно отметить со студентами, что `TypeFilter` внутри по-прежнему сравнивает `getClass().getSimpleName()` —
Strategy убрал разрастание условий в `ListCommand`, но не саму хрупкость сравнения по имени класса. Более надёжная
реализация (например, сверка через `InstrumentFactories` или явный признак типа на `Instrument`) может быть предложена
студентам как самостоятельное улучшение за рамками занятия.

---

## Навыки, полученные на семинаре

`SOLID: LSP` `SOLID: ISP` `SOLID: SRP` `SOLID: OCP` `SOLID: DIP (ознакомительно)` `KISS` `DRY` `picocli`
`pom.xml и управление зависимостями` `паттерн Factory Method` `самрегистрация фабрик по аналогии с JDBC DriverManager`
`паттерн Strategy` `паттерн Iterator` `java.util.Iterator и Iterable` `for-each как синтаксический сахар`
`паттерн Command`
