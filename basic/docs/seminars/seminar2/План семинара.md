# Базовый трек

# Семинар 2. Принципы и паттерны проектирования

**Тема модуля:** Принципы дизайна ПО **Тема семинара:** SOLID. Паттерны GoF в CLI-слое приложения.

---

## Пререквизиты

`семинар 1` `лекция по SOLID, KISS, DRY, GoF` `доменная модель Instrument/Stock/Bond/Etf из семинара 1`
`Maven: базовый уровень`

---

## План семинара

| **#** | **Этап**                                                    | **Время** |
|-------|--------------------------------------------------------------|-----------|
| 1     | Теория: принципы SOLID                                        | ~20 мин   |
| 2     | Обсуждение: какие паттерны GoF применимы к CLI                 | ~15 мин   |
| 3     | CLI-каркас и подключение picocli — показ, с заглушками на потом | ~20 мин   |
| 4     | Совместная реализация: Factory Method                          | ~15 мин   |
| 5     | Итератор по репозиторию — показ и разбор готовой реализации    | ~10 мин   |
| 6     | Самостоятельная работа: Strategy — фильтрация                  | ~20 мин   |
| 7     | Пуш изменений, создание MR                                      | ~5 мин    |

Самостоятельная работа — этап 6 (`InstrumentFilter` по функциональным требованиям, без подсказок). Этап 5 не
пишется совместно заново — Iterator к этому моменту семинара уже реализован; семинарист показывает готовый код и
разбирает его со студентами, не тратя время на повторение уже знакомого по семинару 1 материала. Сначала
обсуждается, какие паттерны GoF сегодня понадобятся (этап 2) — и только после этого показывается CLI-каркас (этап
3), где эти паттерны уже видны в коде: команды `add` и `list` там показаны целиком, но пока не работают до конца —
`add` падает внутри фабрики (этап 4), `list --type` падает внутри фильтра (этап 6). Остальные этапы — совместная
работа с семинаристом.

---

Чеклист семинариста (что подготовить заранее и что отметить по ходу занятия) — в отдельном файле:
[Чеклист](Чеклист%20семинариста.md).

---

### Этап 1. Теория: принципы SOLID (~20 мин)

**Задача этапа:** дать общий словарь для обсуждения качества кода, к которому семинарист будет возвращаться на всех
следующих этапах — Factory Method и Strategy обосновываются через OCP. Мини-лекция по материалу
[«Принципы SOLID»](../../materials/seminar2/Принципы%20SOLID.md), без пересказа целиком.

**Ключевые тезисы:**

- **LSP.** По итогам семинара 1 `getDividends(BigDecimal)` объявлен только в `Stock`
  ([`Stock.java#L36-L39`](../../../src/main/java/academy/backend/market_pulse/model/Stock.java)), а не в `Instrument`, —
  так LSP и не нарушается: `Bond`/`Etf` этот метод не переопределяют неправильно, потому что его для них вообще нет.
  Единственный пробел — эта договорённость нигде не названа явно, узнать о ней можно только прочитав исходный код
  `Stock`.
- **ISP.** Решение пробела выше — назвать способность явно: интерфейс
  [`DividendPaying`](../../../src/main/java/academy/backend/market_pulse/model/DividendPaying.java), реализуемый
  только `Stock`. Код, которому нужны дивиденды, зависит от узкого интерфейса, а не от конкретного класса.
- **SRP.** Класс должен меняться по одной причине. Смешение бизнес-логики и вывода в консоль — частая ошибка именно
  в CLI-командах, к которым семинар подходит на этапе 3.
- **OCP.** Переход от `instanceof`-цепочки к полиморфному `getDescription()` на семинаре 1 — уже пройденный пример
  OCP. Тот же принцип — база для Factory Method (этап 4) и Strategy (этап 6): новый тип или новое правило отбора
  добавляется новым классом, без правки существующего.
- **DIP, обзорно.** Модули верхнего уровня зависят от абстракций, а не от конкретных реализаций. Полноценное,
  автоматизированное применение — тема фреймворков с dependency injection (Spring), она за рамками этого курса и
  разбирается в следующем учебном году.
- **KISS и DRY** — как менее формальные, но практические ориентиры, дополняющие пять принципов SOLID.

---

### Этап 2. Обсуждение: какие паттерны GoF применимы к CLI (~15 мин)

**Задача этапа:** до того как писать код, понять, какие типовые задачи возникают при переходе к CLI, и узнать в них
уже знакомые (или новые) паттерны — вместо того чтобы придумывать решение с нуля на этапе 4.

Формат — открытое обсуждение с группой, а не лекция.

> **Подготовить заранее:** доску (WhiteBoard или доска в Толке) для фиксации таблицы «задача → паттерн-кандидат» по
> ходу обсуждения — по аналогии с доской для брейншторминга домена на семинаре 1.

Вопросы для обсуждения:

- Какие паттерны проектирования уже знакомы студентам?
- Как устроена обработка команды в CLI-приложении — что происходит, когда пользователь вводит `search AAPL`?
- Как создать `Stock`/`Bond`/`Etf`, когда тип инструмента приходит строкой, введённой пользователем, а не из кода?
- Как отфильтровать список инструментов по типу так, чтобы добавление нового типа не требовало правки кода
  фильтрации?
- Как перебрать все инструменты в хранилище, не раскрывая наружу, как именно они там хранятся?

Ожидаемое направление ответов:

| **Задача**                                                        | **Паттерн-кандидат** |
|--------------------------------------------------------------------|-----------------------|
| Обработка пользовательской команды                                 | Command               |
| Создание объекта инструмента по строковому типу                    | Factory Method        |
| Фильтрация списка инструментов без хрупкого сравнения по типу      | Strategy              |
| Обход инструментов в хранилище без раскрытия внутренней структуры   | Iterator              |

Обсуждается, что picocli уже частично реализует Command на уровне библиотеки — аннотированная `@Command` команда,
реализующая `Callable`, фактически объект-команда с методом `call()` в роли `execute()`; конкретный код увидим на
следующем этапе. Iterator в коде живьём не пишется — он уже реализован к этому моменту (этап 5 — показ готового
решения).

После обсуждения — краткий показ материала [«Паттерны проектирования GoF»](./Паттерны%20GoF.md): что такое паттерн
проектирования, три группы, и какой из паттернов-кандидатов из таблицы выше к какой группе относится. Материал не
пересказывается целиком.

---

### Этап 3. CLI-каркас и подключение picocli (~20 мин)

**Задача этапа:** функциональные требования семинара (`search`, `add`, `list`) собираются в CLI — приложение должно
разбирать аргументы командной строки, а не жёстко закодированные вызовы в `demo.Main`. Писать парсер аргументов
вручную избыточно — подключается готовая библиотека
[picocli](https://picocli.info/): декларативное описание команд и опций через аннотации.

Зависимость уже подключена в `pom.xml` заготовки — повод объяснить, что вообще такое `pom.xml` и зачем он нужен
(материал [«Maven и pom.xml»](./Maven.md)), не тратя время на её добавление вживую.

```xml
<dependency>
    <groupId>info.picocli</groupId>
    <artifactId>picocli</artifactId>
    <version>4.7.7</version>
</dependency>
```

Обзор основных типов picocli, без отдельного демонстрационного примера: `@Command` — аннотация класса-команды;
`@Option`/`@Parameters` — именованный/позиционный аргумент; `Callable<Integer>` — интерфейс, реализуемый командой;
`CommandLine` — точка запуска, разбирающая `args` и вызывающая `call()` нужной команды.

Теперь, когда обсудили, какие паттерны сегодня понадобятся, — вот как это выглядит в реальном коде. Показывается
весь CLI-каркас целиком:

- [`MarketPulseCli.java`](../../../src/main/java/academy/backend/market_pulse/cli/MarketPulseCli.java),
  [`demo/Main.java`](../../../src/main/java/academy/backend/market_pulse/demo/Main.java) — корневая команда и точка
  входа: команды регистрируются вручную через `addSubcommand` — у команд нет конструктора без аргументов, каждой
  нужен уже готовый `repository`.
- [`SearchCommand.java`](../../../src/main/java/academy/backend/market_pulse/cli/SearchCommand.java) — уже
  полностью рабочая: поиск по тикеру не опирается ни на один паттерн, который ещё предстоит написать.
- [`AddCommand.java`](../../../src/main/java/academy/backend/market_pulse/cli/AddCommand.java),
  [`ListCommand.java`](../../../src/main/java/academy/backend/market_pulse/cli/ListCommand.java) — код уже написан
  целиком, но запустить `add`/`list --type` пока не выйдет: внутри `StockFactory`/`BondFactory`/`EtfFactory`
  (`add`) и `TypeFilter`/`NoOpFilter` (`list`) на этом этапе — заглушки, которые бросают исключение. Это нормально —
  команды заработают по мере того, как эти классы будут дописаны на этапах 4 и 6.

> **Показать вживую.** Запустить `add STOCK ...` и увидеть `UnsupportedOperationException` из `StockFactory` — это
> не баг, а наглядный повод перейти к вопросу «а как вообще нужно создавать инструмент по строке» на следующем
> этапе.

---

### Этап 4. Совместная реализация: Factory Method (~15 мин)

**Задача этапа:** заглушка `AddCommand` с этапа 3 должна начать создавать `Stock`/`Bond`/`Etf` по типу, пришедшему
строкой; код создания не должен разрастаться в `if/else` при каждом новом типе инструмента.

- [`InstrumentFactory.java`](../../../src/main/java/academy/backend/market_pulse/factory/InstrumentFactory.java) —
  интерфейс фабрики.
- [`StockFactory.java`](../../../src/main/java/academy/backend/market_pulse/factory/StockFactory.java) — одна из
  трёх реализаций (аналогично `BondFactory`, `EtfFactory`).
- [`InstrumentFactories.java`](../../../src/main/java/academy/backend/market_pulse/factory/InstrumentFactories.java) —
  реестр: помимо Factory Method, это ещё и отдельный, тоже именованный паттерн — **Registry** (М. Фаулер, *Patterns
  of Enterprise Application Architecture*): глобальная точка доступа к объектам по ключу, вместо `new` в каждом
  месте использования.

> **Обсуждение:** реестр в `InstrumentFactories` заполняется самрегистрацией — каждая фабрика добавляет себя в
> `static`-блоке при загрузке своего класса (см. `static` в `StockFactory.java#L11-L13`). Это один из способов
> автоматизировать добавление новых фабрик — новый тип не требует правки `InstrumentFactories`. Более глубокое
> обсуждение вариантов автоматической регистрации (в том числе почему `static`-блок сам по себе не гарантирует
> срабатывание и как это решается) — на продвинутом треке.

**Закрываем заглушку:** дописываем тело `create(...)` в `StockFactory`/`BondFactory`/`EtfFactory` — после этого
`add`, показанный на этапе 3, начинает полноценно работать.

---

### Этап 5. Итератор по репозиторию — показ и разбор готовой реализации (~10 мин)

**Задача этапа:** команда `list` должна перебрать все инструменты `InstrumentRepository`, не раскрывая наружу
внутреннее хранение (массив фиксированного размера).

- [`InstrumentRepository.java`](../../../src/main/java/academy/backend/market_pulse/repository/InstrumentRepository.java) —
  хранилище реализует `Iterable<Instrument>`
  ([`L13`](../../../src/main/java/academy/backend/market_pulse/repository/InstrumentRepository.java)), `iterator()`
  возвращает анонимную реализацию `java.util.Iterator`
  ([`L32-L49`](../../../src/main/java/academy/backend/market_pulse/repository/InstrumentRepository.java)).

Показать и обсудить: реализация `Iterable<Instrument>` — условие, необходимое и достаточное для `for-each`; без него
компилятор не примет объект в `for (... : repository)`. Разобрать на паре эквивалентных фрагментов, что `for-each` —
синтаксический сахар над `Iterator`:

```java
void example(InstrumentRepository repository) {
    // то, что пишет программист
    for (Instrument instrument : repository) {
        System.out.println(instrument.getDescription());
    }

    // во что это разворачивается компилятором
    Iterator<Instrument> iterator = repository.iterator();
    while (iterator.hasNext()) {
        Instrument instrument = iterator.next();
        System.out.println(instrument.getDescription());
    }
}
```

---

### Этап 6. Самостоятельная работа: Strategy — фильтрация (~20 мин)

**Задача этапа:** заглушка `ListCommand` с этапа 3 должна начать отбирать инструменты без растущей цепочки `if` в
самой команде — добавление нового критерия отбора не должно требовать правки её кода.

**Интерфейс для реализации:**
[`InstrumentFilter.java`](../../../src/main/java/academy/backend/market_pulse/filter/InstrumentFilter.java) — на
этом этапе в `filter/` только он, без единой готовой реализации. Все функциональные требования (ФТ1–ФТ7) и пара
примеров пользовательского ввода — прямо в его Javadoc, это единственный источник истины для студентов:

- ФТ1. Можно проверить, подходит ли инструмент под правило отбора.
- ФТ2. Есть правило «без отбора» — подходят все инструменты.
- ФТ3. Есть правило «по типу» — подходит инструмент, чей `getType()` совпадает с переданным значением
  ([`Instrument.java`](../../../src/main/java/academy/backend/market_pulse/model/Instrument.java) уже возвращает
  код типа — сравнение не завязано на `getClass().getSimpleName()`); опция `--type` в `ListCommand` уже есть.
- ФТ4. Есть правило «по тикеру» — подходит инструмент, чей тикер содержит переданную подстроку, без учёта регистра:
  `list --ticker AAP` находит `AAPL`.
- ФТ5. Есть правило «по валюте» — подходит инструмент, чья валюта точно совпадает с переданной: `list --currency USD`.
- ФТ6. Есть правило «по цене» — применимо только к `Stock`: для `Bond`/`Etf` не подходит ни при каком значении. В
  роли цены — дивидендная доходность (`Stock.getDividendYield()`), единственный числовой атрибут акции на этом
  этапе проекта. `list --price-op GE --price 10` находит акции с доходностью ≥ 10.
- ФТ7. Критерии взаимоисключающие — одновременно задаётся не больше одного (`--type`, `--ticker`, `--currency` или
  пара `--price-op`/`--price`). Если задано больше одного — это ошибка, а не молчаливая комбинация.

Опции `--ticker`, `--currency`, `--price-op`, `--price` в `ListCommand` пока не заведены — их тоже нужно добавить.

Итоговая реализация (для проверки после самостоятельной работы):
[`TypeFilter.java`](../../../src/main/java/academy/backend/market_pulse/filter/TypeFilter.java),
[`NoOpFilter.java`](../../../src/main/java/academy/backend/market_pulse/filter/NoOpFilter.java),
[`TickerFilter.java`](../../../src/main/java/academy/backend/market_pulse/filter/TickerFilter.java),
[`CurrencyFilter.java`](../../../src/main/java/academy/backend/market_pulse/filter/CurrencyFilter.java),
[`PriceFilter.java`](../../../src/main/java/academy/backend/market_pulse/filter/PriceFilter.java) — и
[`FilterFactory.java`](../../../src/main/java/academy/backend/market_pulse/filter/FilterFactory.java): выбор
конкретного фильтра по опциям CLI и проверка ФТ7 вынесены в отдельную фабрику — вторая практика с Factory Method,
свежепройденным на этапе 4, уже после того, как каждый реализовал фильтры сам.

**Закрываем заглушку:** тело `ListCommand.call()` заменяется на отбор через реализованные фильтры — в эталоне это
вызов `FilterFactory.create(...)`, дальше уже знакомый `for-each` по `repository` с проверкой
`filter.matches(instrument)`.

> **На будущее.** Критерии сегодня взаимоисключающие специально — не «по типу И по цене сразу». Как только
> потребуется комбинировать несколько условий одним вызовом, встаёт задача, знакомая по паттерну **Chain of
> Responsibility**: цепочка обработчиков, каждый из которых решает, пропускать запрос дальше или нет. Сегодня в
> коде его нет — но эта задача для него и существует. Подробнее — на продвинутом треке.

`InstrumentRepository` на данном этапе хранит данные только в памяти процесса (наполняется исключительно через
`add`, ничего не подгружается заранее и не сохраняется между запусками); подключение реального источника данных
запланировано на семинар 5.

---

### Этап 7. Пуш изменений, создание MR (~5 мин)

Организационный этап, к функциональным требованиям проекта не привязан.

```bash
git add src/
git commit -m "feat: SOLID + паттерны GoF в CLI-слое Market Pulse"
git push origin seminar2_wip
```

Создаём MR / Pull Request в веб-интерфейсе. Показываем, как написать описание MR: что сделано и почему именно так.

---

## Навыки, полученные на семинаре

`SOLID: LSP` `SOLID: ISP` `SOLID: SRP` `SOLID: OCP` `SOLID: DIP (ознакомительно)` `KISS` `DRY` `picocli`
`pom.xml и управление зависимостями` `паттерн Factory Method` `паттерн Registry`
`самрегистрация фабрик по аналогии с JDBC DriverManager` `паттерн Strategy` `фильтрация по нескольким критериям`
`паттерн Iterator` `java.util.Iterator и Iterable` `for-each как синтаксический сахар` `паттерн Command`
